names = [ "Ali", "Ahmad", "Junaid", "Jamshaid", "Ahsan"]

for name in names:
    if name.startswith("A"):
        print(f"Found a name that starts with A: {name}")
    if name.startswith("J"):
        print(f"Found a name that starts with J: {name}")