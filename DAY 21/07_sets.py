# set is a collection of well-defined objects
s = {1, 3, 5}

e = set() # Don't use s = {} as it will create an emtpy dictionary
# elements can't be repeated in sets
# sets are immutable
a = {1, 3, 4, 4, 4}
print(a)

