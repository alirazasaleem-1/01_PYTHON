# This program can calculate the area of a circle by getting radius from the user.
import math

pi = math.pi
radius = float(input("Enter the radius of the Circle: "))
area = pi * radius * radius
print(f"The Area of the Circle is : {area}")
