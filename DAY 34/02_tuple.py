# A tuple is like a list but can't be changed
# They are ordered, immutable.

coordinates = (1,2)
print(coordinates)
print(type(coordinates))

# If you try to change them, it will give errors because tuples are immutble.
# coordinates[0] = 3 